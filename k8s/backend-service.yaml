apiVersion: v1
kind: Service

metadata:
  name: cloudops-backend
  namespace: cloudops

spec:
  type: ClusterIP

  selector:
    app: cloudops-backend

  ports:
    - port: 3000
      targetPort: 3000
      protocol: TCP
