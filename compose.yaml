services:
  backend:
    build:
      context: ./backend
      dockerfile: Dockerfile

    container_name: cloudops-backend

    ports:
      - "3000:3000"

    environment:
      NODE_ENV: production
      PORT: 3000

    restart: unless-stopped
